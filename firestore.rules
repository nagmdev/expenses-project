rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Bank-grade multi-tenant expense system
    // Allows read and write for authenticated users (logged in via Email/Password or Google)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
